- [Deployment publish blockers](deployment-publish-blockers.md) — three causes of failed publishes here: invalid `.replit` deploymentTarget value (schema), live secrets in gitignored runtime files bundled into the deploy scan, and a redundant `pip install .` build command failing under the package firewall (uv sync already installs — drop the build step).
- [Deployment security scan & .replit schema](deployment-security-scan.md) — publish fails at Security Scan on filesystem secrets (gitignore doesn't protect snapshot); deploymentTarget must be "vm" not "reserved_vm".
- [Deploy uv venv pollution](deployment-uv-venv-pollution.md) — Python publish fails at "Installing packages" with /nix/store Permission denied when `.pythonlibs` is a non-venv dir; rm -rf .pythonlibs .venv && uv sync.
